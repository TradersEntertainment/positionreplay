{
  "provenance": "SYNTHETIC — generated by scripts/make-csv-fixture.ts",
  "warning": "These numbers are invented. The trades file is shaped like a real exchange export and the klines like a documented Binance response, so the adapter code paths are exercised offline, but they prove nothing about the live Binance contract. Real klines come from `pnpm capture:binance`.",
  "trades": 5,
  "rejectedRows": 1,
  "symbols": {
    "BTC-PERP": "binance BTCUSDT",
    "SHIB-PERP": "uploaded OHLCV (§4.6 fallback)"
  }
}
