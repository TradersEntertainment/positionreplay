# deps
node_modules/
.pnpm-store/

# build output
dist/
.next/
*.tsbuildinfo

# tooling caches
.turbo/
coverage/

# secrets — see SPEC.md §15.2 gotcha 6: this must exist BEFORE the first push
.env
.env.*
!.env.example

# local artifacts
out.png
out/
*.log
.DS_Store
.data/

# screenshots and clips the verify:* scripts write to the repo root for eyeballing
build-*.png
last-*.png
m[0-9]-*.png
m[0-9]-export.*
featured-*.png
sound-*.png
sound-export.*
replay.webm
replay.mp4
replay-again.mp4

# Copied from node_modules at build time by apps/web/scripts/copy-gif-worker.mjs
apps/web/public/gif.worker.js
apps/worker/dist/
